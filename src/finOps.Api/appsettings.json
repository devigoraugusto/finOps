{
    "ConnectionStrings": {
        "DefaultConnection": "${DATABASE_URL}",
        "Redis": "${REDIS_URL}"
    },
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*",
    "CacheRedis": {
        "TimeCache": 60
    }
}
